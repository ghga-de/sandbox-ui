# config parameters for development
host: localhost
port: 8080
welcome_info: |
  This web app is part of an implementation study.
  It only contains basic functionalities and shouldn't be used
  for production.
svc_metadata_url: "http://localhost:8001"
